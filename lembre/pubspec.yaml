name: lembre
description: Lembre App
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1
environment:
  sdk: '>=2.12.0 <3.0.0'
dependencies:
  flutter:
    sdk: flutter
  mobx: ^2.0.1
  flutter_mobx: ^2.0.0
  dio: ^4.0.0-prev1
  cloud_firestore: ^1.0.7
  firebase_core: ^1.1.0
  firebase_auth: ^1.1.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.0.1
  mobx_codegen: ^2.0.1+3
flutter:
  uses-material-design: true